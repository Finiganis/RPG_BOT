package mainBot.utils;

public class JSONSave {
	private static String f = "Save.txt";
	/*
	public static void Save(JSONArray jarray) throws IOException{
		FileWriter fw = new FileWriter(new File(f), false);
        fw.write(jarray.toString());
        fw.close();
	}*/
}
